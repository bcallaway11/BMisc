# check_staggered_inner

A helper function to check if treatment is staggered in a panel data
set.

## Usage

``` r
check_staggered_inner(this_df, treatname)
```

## Arguments

- this_df:

  a data.frame, for this function it should be specific to a particular
  unit

- treatname:

  name of column with the treatment indicator
